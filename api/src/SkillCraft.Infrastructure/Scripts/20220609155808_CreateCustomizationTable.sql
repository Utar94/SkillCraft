START TRANSACTION;

CREATE TABLE "Customizations" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "WorldId" integer NOT NULL,
    "Description" text NULL,
    "Name" character varying(256) NOT NULL,
    "Type" integer NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Uuid" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Customizations" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Customizations_Worlds_WorldId" FOREIGN KEY ("WorldId") REFERENCES "Worlds" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Customizations_CreatedById" ON "Customizations" ("CreatedById");

CREATE INDEX "IX_Customizations_Deleted" ON "Customizations" ("Deleted");

CREATE INDEX "IX_Customizations_Name" ON "Customizations" ("Name");

CREATE INDEX "IX_Customizations_Type" ON "Customizations" ("Type");

CREATE UNIQUE INDEX "IX_Customizations_Uuid" ON "Customizations" ("Uuid");

CREATE INDEX "IX_Customizations_WorldId" ON "Customizations" ("WorldId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220609155808_CreateCustomizationTable', '6.0.5');

COMMIT;
