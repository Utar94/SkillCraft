START TRANSACTION;

ALTER TABLE "Races" ALTER COLUMN "Size" SET DEFAULT 0;

ALTER TABLE "Races" ALTER COLUMN "ExtraLanguages" SET DEFAULT 0;

ALTER TABLE "Races" ALTER COLUMN "ExtraAttributes" SET DEFAULT 0;

CREATE TABLE "Conditions" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "WorldId" integer NOT NULL,
    "Description" text NULL,
    "Name" character varying(256) NOT NULL,
    "MaxLevel" integer NOT NULL DEFAULT 0,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Uuid" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Conditions" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Conditions_Worlds_WorldId" FOREIGN KEY ("WorldId") REFERENCES "Worlds" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Conditions_CreatedById" ON "Conditions" ("CreatedById");

CREATE INDEX "IX_Conditions_Deleted" ON "Conditions" ("Deleted");

CREATE INDEX "IX_Conditions_Name" ON "Conditions" ("Name");

CREATE UNIQUE INDEX "IX_Conditions_Uuid" ON "Conditions" ("Uuid");

CREATE INDEX "IX_Conditions_WorldId" ON "Conditions" ("WorldId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220620144224_CreateConditionTable', '6.0.5');

COMMIT;
