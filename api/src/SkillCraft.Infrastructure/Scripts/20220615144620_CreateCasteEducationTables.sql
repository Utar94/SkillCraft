START TRANSACTION;

CREATE TABLE "Castes" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "WorldId" integer NOT NULL,
    "Description" text NULL,
    "Name" character varying(256) NOT NULL,
    "Skill" integer NOT NULL,
    "WealthRoll" character varying(10) NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Uuid" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Castes" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Castes_Worlds_WorldId" FOREIGN KEY ("WorldId") REFERENCES "Worlds" ("Id") ON DELETE CASCADE
);

CREATE TABLE "Educations" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "WorldId" integer NOT NULL,
    "Description" text NULL,
    "Name" character varying(256) NOT NULL,
    "Skill" integer NOT NULL,
    "WealthMultiplier" integer NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Uuid" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Educations" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Educations_Worlds_WorldId" FOREIGN KEY ("WorldId") REFERENCES "Worlds" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Castes_CreatedById" ON "Castes" ("CreatedById");

CREATE INDEX "IX_Castes_Deleted" ON "Castes" ("Deleted");

CREATE INDEX "IX_Castes_Name" ON "Castes" ("Name");

CREATE UNIQUE INDEX "IX_Castes_Uuid" ON "Castes" ("Uuid");

CREATE INDEX "IX_Castes_WorldId" ON "Castes" ("WorldId");

CREATE INDEX "IX_Educations_CreatedById" ON "Educations" ("CreatedById");

CREATE INDEX "IX_Educations_Deleted" ON "Educations" ("Deleted");

CREATE INDEX "IX_Educations_Name" ON "Educations" ("Name");

CREATE UNIQUE INDEX "IX_Educations_Uuid" ON "Educations" ("Uuid");

CREATE INDEX "IX_Educations_WorldId" ON "Educations" ("WorldId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220615144620_CreateCasteEducationTables', '6.0.5');

COMMIT;
