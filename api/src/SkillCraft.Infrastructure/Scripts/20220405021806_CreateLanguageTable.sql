START TRANSACTION;

CREATE TABLE "Languages" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Description" text NULL,
    "Exotic" boolean NOT NULL DEFAULT FALSE,
    "Name" character varying(100) NOT NULL,
    "Script" character varying(100) NULL,
    "TypicalSpeakers" character varying(100) NULL,
    "WorldId" integer NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "Key" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Languages" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Languages_Worlds_WorldId" FOREIGN KEY ("WorldId") REFERENCES "Worlds" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Languages_CreatedById" ON "Languages" ("CreatedById");

CREATE INDEX "IX_Languages_Deleted" ON "Languages" ("Deleted");

CREATE UNIQUE INDEX "IX_Languages_Key" ON "Languages" ("Key");

CREATE INDEX "IX_Languages_Name" ON "Languages" ("Name");

CREATE INDEX "IX_Languages_Script" ON "Languages" ("Script");

CREATE INDEX "IX_Languages_TypicalSpeakers" ON "Languages" ("TypicalSpeakers");

CREATE INDEX "IX_Languages_WorldId" ON "Languages" ("WorldId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220405021806_CreateLanguageTable', '6.0.3');

COMMIT;
